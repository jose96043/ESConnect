/*global define*/

define([
    'underscore',
    'backbone'
], function (_, Backbone) {
    'use strict';

    var ConnectmodelModel = Backbone.Model.extend({
        defaults: {
        }
    });

    return ConnectmodelModel;
});
